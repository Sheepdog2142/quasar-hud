import React from 'react';
import type { RequestsInfo } from '../../types';
interface RequestsBarProps {
    requests: RequestsInfo;
}
declare const RequestsBar: React.FC<RequestsBarProps>;
export default RequestsBar;
