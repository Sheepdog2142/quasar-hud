{
  "name": "quasar-hud",
  "version": "0.1.0",
  "description": "Persistent terminal HUD for AI coding CLIs — Copilot, Claude, and Codex",
  "main": "dist/index.js",
  "bin": {
    "qhud": "dist/index.js",
    "qhud-setup": "dist/setup.js"
  },
  "scripts": {

    "dev": "ts-node --transpile-only src/index.tsx",
    "build": "tsc",
    "start": "node dist/index.js",
    "launch:copilot": "pwsh -NonInteractive -File scripts\\launch-copilot.ps1",
    "launch:claude": "pwsh -NonInteractive -File scripts\\launch-claude.ps1",
    "launch:codex": "pwsh -NonInteractive -File scripts\\launch-codex.ps1",
    "launch:inline:copilot": "ts-node --transpile-only src/index.tsx -- --cli=copilot",
    "launch:inline:claude": "ts-node --transpile-only src/index.tsx -- --cli=claude",
    "launch:inline:codex": "ts-node --transpile-only src/index.tsx -- --cli=codex",
    "setup": "node dist/setup.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "ink": "^3.2.0",
    "js-yaml": "^4.1.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  },
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=18.0.0"
  }
}
